---
date: 2026.05.25
client: 2.2.7
server: 1.6.2
---

# 更新说明

## 高危漏洞修复

### 后端投票限额判断错误

错误定位点`routes/vote.js`

```JavaScript
-     const isCopyTeam = user.group === "文案组";
+     const isCopyTeam = user.Status.name === "文案组";
```

### 统一认证端口错误

定位错误点`middleware/auth.js`

```JavaScript
+   if (user.isBanned) {
+     return res.status(403).json({ message: "账号已被封禁，请联系管理员" });
+   }
+   if (!user.isApproved && user.Status.name !== "admin") {
+     return res.status(403).json({ message: "账号待审核，请等待管理员通过" });
+   }
```

## 常规修复

### 删除废弃后端路由

定位错误点`routes/vote.js`

```JavaScript
- /issue/:id/show-list
```
